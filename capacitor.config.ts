import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agnibinasangha.braytracker',
  appName: 'Agnibina Sangha',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
